apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 80
    name: http
  selector:
    name: result-app-pod
    app: demo-voting-app